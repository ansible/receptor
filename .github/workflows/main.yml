---
name: CI

on:
  pull_request:
  push:
    branches: [devel]

jobs:
  build:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Run CI tasks in Docker
        uses: ./build/ci/

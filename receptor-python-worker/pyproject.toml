[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "receptor-python-worker"
dynamic = ["version"]

[project.optional-dependencies]
dev = [
    "nox",
]

# Vagrant environment

Vagrant environment for Receptor dev and testing.

## Requirements

- Vagrant
- Ansible

## Commands

```bash
# Create VM
vagrant up

# SSH into VM
vagrant ssh
cd /vagrant

# Destroy VM
vagrant destroy

# Reapply playbook.yaml
vagrant provision
```
